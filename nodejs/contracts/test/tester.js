const { anyValue } = require('@nomicfoundation/hardhat-chai-matchers/withArgs');
const { expect } = require('chai');

describe('PancakeFlashSwap', function () {
  it('prints hello', () => {
    console.log('hello');
  });
});
